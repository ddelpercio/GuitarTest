@model IEnumerable<GuitarMate.Models.GuitarPlayer>

@{
    ViewBag.Title = "Index";
}

<h2>Search Results</h2>
<!--
<p>
    @Html.ActionLink("Create New", "Create")
</p>-->

    <table class="table hovertable">
        <tr>
            <!--     <th>
                      @Html.DisplayNameFor(model => model.PlayerName)
                  </th>-->
            <th>
                
                    @Html.DisplayNameFor(model => model.Location)
               
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Instrument)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AdDescription)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            
                <tr onclick="location.href='@Url.Action("Details", "GuitarPlayers", new { id = item.ID })'">
                    
                        <!--     <td>
            @Html.DisplayFor(modelItem => item.PlayerName)
        </td>-->
                        <td>
                            @Html.DisplayFor(modelItem => item.Location)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Instrument)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.AdDescription)
                        </td>
                        <td>
                            <!--  @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |-->
                            <!-- @Html.ActionLink("See More", "Details", new { id=item.ID }) -->
                            <!--  @Html.ActionLink("Delete", "Delete", new { id = item.ID })-->
                        </td>
                    
                </tr>
            
        }

</table>

